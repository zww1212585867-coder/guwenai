# 复制为 .env 并填入真实值（.env 已被 gitignore，绝不会进仓库）

# ====== 必填：两个 key 都建议填 ======
# DeepSeek：国内 VPS 主力模型（便宜 + 不被墙）
# Gemini：备用兜底（国内 VPS 常连不上，但挂了它 DeepSeek 故障时不会全军覆没）
# ★ 任意一个为空，对应模型在对话中就不会被调用 ★
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx
GEMINI_API_KEY=

# 服务监听
PORT=3000
HOST=0.0.0.0
