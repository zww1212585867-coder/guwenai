# 复制为 .env 并填入真实值（.env 已被 gitignore，绝不会进仓库）
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx

# 可选：Gemini 免费额度（年费会员）作为兜底。不填则纯 DeepSeek。
# 启用方式见 README「模型切换」——把 config.yaml 的 model 段改成 Gemini 即可。
GEMINI_API_KEY=

# 服务监听
PORT=3000
HOST=0.0.0.0
